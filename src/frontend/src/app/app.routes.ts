import { Routes } from '@angular/router';
import { MediaConfigsComponent } from './media_configs/media_configs.component';

export const routes: Routes = [
    { path: 'settings', component: MediaConfigsComponent }
];
