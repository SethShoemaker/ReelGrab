import { SeasonEpisodeFormatPipe } from './season-episode-format.pipe';

describe('SeasonEpisodeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonEpisodeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
