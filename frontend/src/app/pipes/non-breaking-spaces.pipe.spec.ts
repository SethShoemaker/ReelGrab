import { NonBreakingSpacesPipe } from './non-breaking-spaces.pipe';

describe('NonBreakingSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new NonBreakingSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
